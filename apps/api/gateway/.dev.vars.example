# ─── Secrets ───────────────────────────────────────────────────────────────────
# Copy this file to .dev.vars and fill in your values.

JWT_SECRET="a_random_secret_key_at_least_32_chars_long"
# Must match the JWT_SECRET used by api-agent for token verification.

# Bindings from wrangler.toml (resolved automatically):
#   SESSIONS_KV (KV), DB (D1)
#   AGENT_WORKER, DOCGEN_WORKER, PAYMENTS_WORKER (service bindings)
